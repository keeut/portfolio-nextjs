import React from 'react';

const PortfolioText = (props) => (
    <div>
        <p>포트폴리오 용도로 제작한 웹사이트입니다. 현재 이 웹사이트에 해당합니다.</p>
        <p>처음에 React로 구현하였습니다. 하지만 이후 정적인 사이트를 굳이 CSR로 할 필요가 없다고 판단해 static generation인 Next.js으로 변경하여 구현하였습니다.</p>
        <p>반응형 CSS인 media query를 이용해 Resposive Web으로 디자인하였습니다.</p>
    </div>   
    );

export default PortfolioText;