import React from 'react';

const TechStack = (props) => (
        <div>
            <div>{props.name}</div>
            <img src="../../../img/front-end.jpg" alt='sdf'/>
        </div>
    );

export default TechStack;